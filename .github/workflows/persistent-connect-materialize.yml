name: Persistent CONNECT materialization

on:
  pull_request:
    branches: [feat/unified-node-provider-host-clean, main]

permissions:
  contents: write

concurrency:
  group: persistent-connect-materialize
  cancel-in-progress: true

jobs:
  materialize:
    if: github.event.pull_request.head.repo.full_name == github.repository && github.head_ref == 'feat/persistent-connect-listener'
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: feat/persistent-connect-listener
          fetch-depth: 0
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 24
          cache: pnpm
      - run: python3 scripts/_tmp_persistent_connect.py
      - run: pnpm install --frozen-lockfile
      - run: pnpm run audit
      - run: pnpm run typecheck
      - run: pnpm test
      - run: pnpm run build
      - name: Commit validated source patch
        shell: bash
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git rm .github/workflows/persistent-connect-materialize.yml scripts/_tmp_persistent_connect.py
          git add src/node.ts src/core/index.ts src/warp/persistent.ts tests/persistent-warp.test.ts tests/node-connect-listener.test.ts
          git diff --cached --check
          git commit -m "feat(warp): attach CONNECT to the persistent PXPipe listener"
          git push origin HEAD:feat/persistent-connect-listener
