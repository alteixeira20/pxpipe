name: Stabilization Upstream Snapshot

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  snapshot:
    if: github.head_ref == 'stabilize/upstream-snapshot-20260811'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - name: Pack exact checkout
        run: tar --exclude=.git -czf /tmp/pxpipe-upstream-source.tgz .
      - name: Upload checkout
        uses: actions/upload-artifact@v4
        with:
          name: pxpipe-upstream-source
          path: /tmp/pxpipe-upstream-source.tgz
          if-no-files-found: error
          retention-days: 1
